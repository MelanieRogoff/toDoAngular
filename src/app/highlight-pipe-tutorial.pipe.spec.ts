import { HighlightPipeTutorialPipe } from './highlight-pipe-tutorial.pipe';

describe('HighlightPipeTutorialPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightPipeTutorialPipe();
    expect(pipe).toBeTruthy();
  });
});
